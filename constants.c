#include <stdio.h>

const int minutes_per_hour = 60;
const float PI = 3.14;

void main() {
  printf("int math the value of PI is %f\n", PI);
  printf("int real life minutes per hour indicate with %d\n", minutes_per_hour);
}